import React from "react";
interface Props {
    subject: string;
    notes: string;
}
declare const LeaveNotesPopover: ({ subject, notes }: Props) => React.JSX.Element;
export default LeaveNotesPopover;
