{
  "name": "leather-works",
  "version": "1.0.0",
  "private": true,
  "description": "Leather Works ERP monorepo",
  "type": "module",
  "scripts": {
    "install:all": "npm install && npm install --prefix server && npm install --prefix client && npm install --prefix electron",
    "build": "npm run build:shared && npm run build:server && npm run build:client && npm run build:electron",
    "build:shared": "tsc -b shared",
    "build:server": "npm --prefix server run build",
    "build:client": "npm --prefix client run build",
    "build:electron": "npm --prefix electron run build",
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\" \"npm run dev:electron\"",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm --prefix client run dev",
    "dev:electron": "npm --prefix electron run dev",
    "clean": "rm -rf shared/dist server/dist client/dist electron/dist-electron",
    "typecheck": "npm run typecheck:shared && npm run typecheck:server && npm run typecheck:client && npm run typecheck:electron",
    "typecheck:shared": "tsc -b shared",
    "typecheck:server": "npm --prefix server run typecheck",
    "typecheck:client": "npm --prefix client run typecheck",
    "typecheck:electron": "npm --prefix electron run typecheck"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "typescript": "^5.7.3"
  },
  "workspaces": [
    "shared",
    "server",
    "client",
    "electron"
  ]
}
